import React from "react";

const ComplexDefaultDashboardPage = () => {
  return <div>ComplexDefaultDashboardPage</div>;
};

export default ComplexDefaultDashboardPage;

// this is the default page for the complex dashboard route.
// It is used when no specific parallel route is defined.
// It serves as a fallback or default view for the complex dashboard layout.