const Loading = () => {
  return (
    <div>
      <h3>Loading</h3>
    </div>
  );
};

export default Loading;
// This component will be displayed while the blog page is loading.
// It is automatically used by Next.js when the page is being fetched.

// You can customize the loading state by modifying this component.
// For example, you can add a spinner or skeleton loader to indicate that content is being loaded