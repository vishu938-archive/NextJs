import Footer from "@/components/footer";

export default function MarketingLayout({ children }) {
  return (
    <html lang="en">
      <body>
        {/* {children} */}
        {/* <Footer /> */}
      </body>
    </html>
  );
}

// try creating separate folder structure similar to nextJs app to understand about Multiple root layouts.
