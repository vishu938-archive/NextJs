export const metadata = {
  title: "About page details"
}

const About = () => {
  return <div>About</div>;
};

export default About;

// title present in this metadata is overriden by rootlayout title.
// this is static metadata